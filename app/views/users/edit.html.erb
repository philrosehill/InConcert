<h1>Edit a User</h1>

<%= simple_form_for @user, html: {class: "form-horizontal"} do |form| %>
	<%= form.input :name %>
	<%= form.input :user_zip_code %>
	<%= form.input :rank %>
	<%= form.submit "Update User", class: "btn btn-primary" %>
<% end %>